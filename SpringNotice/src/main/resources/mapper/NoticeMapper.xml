<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.tobi.springnotice.mapper.NoticeMapper">

    <insert id="insertNotice" parameterType="com.example.tobi.springnotice.model.User">
        INSERT INTO user(userid, title , content)
        VALUES (#{userid}, #{title}, #{content});
    </insert>

        </mapper>